import 'package:flutter/material.dart';

class DataEntryView extends StatelessWidget {
  const DataEntryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DataEntryView'),
      ),
    );
  }
}
